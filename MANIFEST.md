# DevDocs — file manifest

Static, self-contained documentation site teaching HTML, CSS, JavaScript, TypeScript, and Angular
(basics → advanced). No CDN, no build step. Open `index.html` by double-clicking it.

| File | Purpose |
|------|---------|
| `index.html` | Landing / table of contents. Overview, "how to use", syllabus cards linking each tech page, live mini-playground, **two use-case maps** — `#use-case-employee-dashboard` and `#use-case-typed-employee` — each linking a use case's requirements to the page/anchor that teaches it. 8 sections. |
| `html.html` | HTML guide — **24 sections** (~2600 lines, 51 live examples). Core + dashboard sections, **+ deep gap-fill: data-tables, audio/video (+`<track>`), embedded-content (iframe sandbox/`object`), character-references, global-attributes (`contenteditable`/`inert`/`hidden`…), advanced-form-controls (`datalist`/`output`/`progress`/`meter` + Constraint Validation API), dialog-popover (`<dialog>`/Popover API), inline-svg, resource-loading (`defer`/`async`/`module`, `preload`/`prefetch`), drag-and-drop**. |
| `css.html` | CSS guide — **26 sections** (~2800 lines, 62 live examples). Core + dashboard sections, **+ deep gap-fill: values-and-units, typography (`@font-face`/variable fonts), colors-and-gradients (oklch/`color-mix`), sizing-object-fit, backgrounds-borders-shadows, overflow-and-scroll (scroll-snap), advanced-selectors-pseudos (`:has`/`:is`/`:where`/`::marker`…), css-nesting-scope (`&`/`@scope`), feature-and-advanced-media-queries (`@supports`/`prefers-*`), filters-blend-clip-mask, more-topics**. |
| `javascript.html` | JS guide — **27 sections** (~2700 lines, 56 live examples). Core + dashboard sections, **+ deep gap-fill: this-and-binding, prototypes-and-classes (`#private`/static/mixins), error-handling (custom errors/`cause`/`AggregateError`), iterators-and-generators (async generators), regular-expressions, object-model-immutability (descriptors/`freeze`), more-topics** (regex/proxy/observers/timers/URL/files/security pointers). |
| `typescript.html` | TS guide — **35 sections** (~2800 lines, 96 code-blocks + 13 runnable). Core + employee-flow sections, **+ deep gap-fill: classes-advanced (abstract/`implements`/parameter-props/`override`/polymorphic `this`), type-assertions-and-casting, utility-types-catalog, enums-deep, tuples-and-variadic, satisfies-and-const-context, modules-and-import-type, template-literal-types, type-level-programming (recursive conditional types), variance-and-advanced-generics (`in`/`out`), control-flow-narrowing, more-topics**. Source + simulated `tsc`/runtime output (no compiler bundled offline). |
| `angular.html` | Angular guide — **44 sections** (~2640 lines, 145 code+output pairs). Basics → advanced, Angular 17–22: introduction/why/setup, components, `@Input`/`@Output`, two-way binding, block control flow (`@if`/`@for`/`@switch`/`@let`), legacy structural directives, `ngClass`/`ngStyle`, `ng-container`, attribute & custom structural directives, `ng-template`/`ngTemplateOutlet`, ViewChild & content queries, content projection, view encapsulation, **SCSS architecture & theming (light/dark via CSS variables)**, pipes, services, `HttpClient` + async pipe, **HTTP interceptors & global error handling**, dependency injection + `inject()`, change detection, lifecycle hooks, NgModules, standalone components, **reusable & shared UI (container vs presentational)**, routing & guards, forms, RxJS, `@defer`, Signals, signal `input()`/`output()`/`model()`, Resource API, zoneless reactivity, SSR/hydration/i18n/Elements, testing, **plus a `#use-case-employee-portal` map** linking an Employee Management Portal's requirements to the section that teaches each. Static code + **side-by-side simulated `Output`** (no Angular compiler bundled offline) via the `.io-grid` layout. |
| `angular-core-deep-dive.html` | **Standalone** self-contained study guide (own inline CSS/JS/SVG, not the DevDocs shell) — **19 sections / 115 lecture articles** (~8600 lines). Original paraphrase of the Angular Core Deep Dive course structure (no transcript text reproduced), Angular 17–22. Per-lecture objective, key points, **side-by-side code+output** (174 pairs), pitfalls, and advanced notes; per-section quizzes and cheat-sheets; **54 inline SVG diagrams**; sticky filtered TOC, theme toggle, inline highlighter, print CSS. Examples reimplemented + branch-cited from `angular-university/angular-course`. Audited and improved (correctness fixes, added diagrams/examples, deepened explanations). Linked from the homepage nav as "Angular Deep Dive". |
| `web-design-html-css-js.html` | **Standalone** self-contained study guide (own inline CSS/JS, not the DevDocs shell) — **8 sections / 77 lectures** (~5500 lines, 405 KB). Original paraphrase of a beginners' *Web Design: HTML, CSS & JavaScript* course: HTML foundations, CSS styling & responsive layout (Flexbox/Grid), JavaScript fundamentals, the DOM, a full responsive-website build, practical JS techniques (arrow functions, `.every()`, closures, JSON, fetch, image gallery), and advanced exercises (custom `map`/`debounce`, a mini promise, deep clone). Because HTML/CSS/JS run natively, examples are **live and editable** — **67 sandboxed-iframe runners** (edit-and-re-run) rather than simulated — plus **32 inline SVG diagrams**, per-section quizzes (6 questions each) and cheat-sheets, per-lecture objective/key-points/pitfalls, a sticky filtered TOC, theme toggle, inline highlighter, and print CSS. Concepts paraphrased and examples reimplemented from the course PDFs (transcript was mixed English/Italian auto-captions — reference only; source folder is gitignored). Linked from the homepage nav as "Web Design". |
| `cheatsheets.html` | Cheatsheets landing (shared DevDocs shell) — links to the two quick-reference pages below with a short "what's inside" list for each. Pinned **last** in the top nav (new cheatsheets are added here over time). |
| `cheatsheet-angular.html` | **Standalone** Angular quick-reference — **16 categories / 121 concept cards** (~152 KB). Each card = a one-line reminder + a minimal syntax snippet + its output (+ a diagram where it helps), no prose. Structure mirrors the **Angular Core Deep Dive (primary source)**: introduction, components & core directives & pipes, template querying, content projection, templates, directives, view encapsulation, services & HttpClient, dependency injection, change detection, lifecycle, modules & custom pipes, i18n, standalone/Elements/@defer, and Signals — plus a final "Beyond the core" category (forms, routing, RxJS, testing) drawn from `angular.html`. Sticky filtered TOC (filters cards live), theme toggle, inline highlighter (html/css/js/ts/bash), print CSS, 20 inline SVG diagrams. |
| `cheatsheet-web-design.html` | **Standalone** HTML/CSS/JS quick-reference — **8 categories / 88 concept cards** (~98 KB). Same card format. Structure mirrors the **Web Design course (primary source)** in its own order: HTML foundations, styling with CSS, JavaScript fundamentals, the DOM, building a responsive site, JavaScript in practice, advanced JavaScript, and publishing — with the HTML/CSS/JS reference guides filling gaps. Same shell (filtered TOC, theme, highlighter, print, 12 SVG diagrams). |
| `styles/site.css` | Shared docsite stylesheet — layout, two-column example grid, light/dark theme variables, syntax-token colors, responsive rules. UI only. |
| `scripts/site.js` | Shared docsite runtime — builds each example's live `iframe` (`srcdoc` = HTML+CSS+JS with an injected console panel), Run/Reset buttons, editor tabs, TOC filter, theme toggle, scroll-spy, regex syntax highlighter. UI only — not the JavaScript lessons. |
| `MANIFEST.md` | This file. |

## How the live examples work
Each `.example` = editable code on the left, sandboxed `<iframe>` preview on the right.
`site.js` reads the `textarea.code[data-lang]` editors, assembles a document, and sets
`iframe.srcdoc`. JS-only examples log to an injected console panel. Edit any code and
click **Run** (or `Ctrl/Cmd+Enter`); **Reset** restores the original.

## Note on structure
The build uses **separate pages per technology** (`html.html`, `css.html`, `javascript.html`,
`typescript.html`) per the spec's "each technology must be a separate HTML page" — not the
combined `javascript_typescript.html` mentioned once later in the same spec.
